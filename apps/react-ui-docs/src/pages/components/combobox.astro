---
import { getEntry, render } from 'astro:content';
import DocumentLayout from '@layouts/DocumentLayout.astro';

const entry = await getEntry('components', 'ComboBox');
if (!entry) {
	throw new Error('Could not find ComboBox component documentation');
}

const { Content, headings } = await render(entry);
---

<DocumentLayout title="ComboBox Component | React UI" headings={headings}>
	<Content />
</DocumentLayout>
