---
import { getEntry, render } from 'astro:content';
import DocumentLayout from '@layouts/DocumentLayout.astro';

const entry = await getEntry('components', 'DatePicker');
if (!entry) {
	throw new Error('Could not find DatePicker component documentation');
}

const { Content, headings } = await render(entry);
---

<DocumentLayout title="DatePicker Component | React UI" headings={headings}>
	<Content />
</DocumentLayout>
