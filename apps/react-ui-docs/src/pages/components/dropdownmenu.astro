---
import { getEntry, render } from 'astro:content';
import DocumentLayout from '@layouts/DocumentLayout.astro';

const entry = await getEntry('components', 'DropDownMenu');
if (!entry) {
	throw new Error('Could not find DropDownMenu component documentation');
}

const { Content, headings } = await render(entry);
---

<DocumentLayout title="DropDownMenu Component | React UI" headings={headings}>
	<Content />
</DocumentLayout>
