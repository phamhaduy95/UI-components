---
import { getEntry, render } from 'astro:content';
import DocumentLayout from '@layouts/DocumentLayout.astro';

const entry = await getEntry('components', 'ScrollArea');
if (!entry) {
	throw new Error('Could not find ScrollArea component documentation');
}

const { Content, headings } = await render(entry);
---

<DocumentLayout title="ScrollArea Component | React UI" headings={headings}>
	<Content />
</DocumentLayout>
