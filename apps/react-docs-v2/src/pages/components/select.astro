---
import { getEntry, render } from 'astro:content';
import DocumentLayout from '@layouts/DocumentLayout.astro';

const entry = await getEntry('components', 'Select');
if (!entry) {
	throw new Error('Could not find Select component documentation');
}

const { Content, headings } = await render(entry);
---

<DocumentLayout title="Select Component | React UI" headings={headings}>
	<Content />
</DocumentLayout>
