---
import { getEntry, render } from 'astro:content';
import DocumentLayout from '@layouts/DocumentLayout.astro';

const entry = await getEntry('components', 'DateRangePicker');
if (!entry) {
	throw new Error('Could not find DateRangePicker component documentation');
}

const { Content, headings } = await render(entry);
---

<DocumentLayout title="DateRangePicker Component | React UI" headings={headings}>
	<Content />
</DocumentLayout>
